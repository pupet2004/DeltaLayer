# DeltaLayer：不要保存上下文，保存变化

## 一种面向长期 AI Agent 的极简连续层

*DeltaLayer: Persist Change, Not Context — A Minimal Continuity Layer for Long-Running AI Agents*

2026-09-21 · Design Note / Open Proposal · v0

> **Persist change, not context. Let the next model reconstruct the rest.**
>
> 保存变化，而不是保存上下文；剩下的，让下一任模型自己重建。

本文提出的是一个待验证的设计，不是成熟产品宣言。已有实验支持它作为轻量项目入口的可行性，但还没有证明它能显著降低长期 Agent 工作的总体成本。

## 1. 为什么每次续接都像重新入职

在我们的长期开发工作里，一个熟悉的场景反复出现：上一任 Agent 已经理解项目，下一任却还要重新看代码、重新问背景、重新理解为什么没有采用另一个看似合理的方案。

源码能回答“现在实现了什么”，却不一定直接说明“为什么变成这样”。一次已经被否定的架构、一个验证失败后收紧的边界、一条被明确禁止自动启动的产品方向，都可能影响接下来的判断。

当然可以保留整段对话，也可以认真写 handoff。问题不是它们不能用，而是：**为了继续做事，下一任 Agent 必须继承多少过去？**

我们想尝试更小的单位。

## 2. 两条工作线收敛到一个问题

这项设计来自两条工作线。Workbench 的工程实践让我们开始关注：Agent 能不能在正常回答之外，额外输出一份结构化的 `changes[]`？QCT 的真实开发历史则提供了另一种检验：如果只保留这些变化，新的 Agent 能理解多少，又能否接着开发？

两条线指向同一个接口：正常完成任务，同时留下“项目从此有什么不同”。

这里必须区分证据：Workbench 是作者报告的工程观察，本发布包尚未附上其独立可复核材料；下面的定量结果来自 QCT，48 条历史变化也是**事后回填**，不是对当时在线写入能力的直接测量。两者不能混成一次更大规模的实验。

如果这种额外输出不依赖财务分析或某一种工作台业务，它也许值得被当成项目连续性的基础构件来检验。

## 3. 保存的不是发生过什么，而是什么改变了

“运行了测试”和“验证状态不再在 History reopen 后丢失”，不是同一种信息。

前者描述操作，后者改变了未来工作的前提。

DeltaLayer 把 change 定义为：

> 某个时刻以后，项目与之前相比发生了什么具有持续意义的变化。

它可以是能力增加、缺陷修复、架构调整、决策替换，也可以是旧假设失效：

```text
此前认为工具注册表已经约束参数；
实际发现执行边界未校验，现已补上严格校验。
```

它不是工具日志，不是压缩版推理过程，也不要求每个任务都写一段总结。只做调查、没有形成值得长期保留的新事实时，`changes: []` 就是正确输出。

判断标准很朴素：**未来 Agent 即使永远看不到本次对话，是否仍然值得知道这件事？**

但短不是唯一目标。若没有写下原因，后来的模型就不能凭空恢复原因。关键约束、替换理由、适用范围应该与变化一起留下；“修好了”通常不是足够好的记录。

## 4. 最小设计只有三个文件

```text
project/
├── AGENTS.md
├── PROJECT.md
└── .deltalayer/
    └── changes.jsonl
```

`AGENTS.md` 约定怎样读取和写入。

`changes.jsonl` 保存长期语义变化。每条记录包含写入时间、来源和 `changes[]`，可选地附上原对话标识。它只追加，不因为后来的结论改变就改写过去。

`PROJECT.md` 保存方便启动的当前视图：目标、架构、阶段、约束和开放问题。它可以更新、过时或被重新生成，不是不可质疑的权威。

```text
changes.jsonl = durable semantic history
PROJECT.md    = disposable current view
```

这里的“可重建”是语义目标，不是逐字重放保证。遗漏的历史仍可能需要源码、Git 或人来补足；LLM 也可能重建错。首次接入既有项目时，不能只写一份独有的 `PROJECT.md`，再声称删掉它也不会丢信息，重要基线同样需要进入可保留的历史。

这个三个文件的设计是实验后收敛出的 v0 提案。**已有 A/B/C 实验没有完整验证 `PROJECT.md + changes` 这一组合。**

## 5. 新 Agent 从最近的变化开始

启动路径是：

```text
PROJECT.md
    ↓
recent changes
    ↓
不够 → older changes → 再判断
    ↓
足够 → 读取任务相关源码 → 验证 → 工作
```

v0 不把 `top_k = 10` 之类的固定深度当成正确答案。它把问题交回模型：当前上下文是否足以安全、正确地继续？

实验中出现过 `3 → 8 → stop`，也出现过 `3 → stop`。这只是几个实际观察，不代表“通常三条就够”，更不能证明模型总能准确判断自己还缺什么。

停止读历史不等于停止验证。模型可以已经理解项目方向，却仍然需要检查接口、读取测试、确认数据结构。DeltaLayer 想减少的是为了找回背景而重复探索，不是正常的软件工程工作。

## 6. 历史矛盾为什么可以先保留

考虑一段简单演化：

```text
09:30 选择 A。
11:10 A 无法满足约束，改为 B。
14:20 B 验证失败，最终采用 C。
```

没有必要为了生成一份“永远正确的当前记忆”，把 A 和 B 从历史里清掉。保留替换关系，后来的模型才有机会知道 C 为什么存在，也可能避免再次走回 A。

v0 因此不要求预先建立一套语义冲突消解器。它要验证的是：对于表达清楚的变化，模型能否在读取时理解废弃、替代与因果。

这不是“所有冲突都已解决”。两个 Agent 同时作出互斥决定时，时间戳不代表授权顺序，最后写入也不自动代表最终决策。模型必须核对现实、承认未知，必要时询问负责人。文件并发写入的完整性同样是实现问题，不会因为模型理解语义就自行消失。

## 7. 它为什么可能省 token

我们的设想不是把所有旧对话塞进更大的窗口，而是让未来的工作只携带对它仍有意义的变化。

如果大量历史是临时调查、重复工具输出和已失效的尝试，语义变化可能是一种更紧凑的入口。但这是一种有损选择，不是无损压缩：被丢弃的细节可能恰好是下一次任务需要的。

因此真正应该测的是：

```text
总成本 = 变化生成与维护
       + 历史检索与解释
       + 源码核实
       + 遗漏或误导造成的返工
```

少读历史不必然意味着少花 token。若短记录让模型多查源码、误判方向或反复重建视图，整体可能更贵。

## 8. 实验告诉了我们什么

下面数据来自随包提供的 [实验记录](EXPERIMENTS.md)。它们是探索性单次样本，不是独立 benchmark。

### 48 个真实任务，事后回填

我们从真实成功任务历史中选取 48 个有开发意义的任务，各自回填一个包含来源、完成时间和变化的 Delta。无持久变化的任务也保留空数组。

| 同组材料 | 字符数 |
| --- | ---: |
| Task 最终消息拼接 | 40,555 |
| 紧凑 Delta JSON，含最小 metadata | 10,398 |
| 纯 `changes` 文本 | 4,585 |

纯变化文本约为最终消息的 11.3%；含 metadata 的 Delta 约为 25.6%。

这是 Python 字符串长度口径，不是字节数，也不是 token 数。分母是同组任务的最终消息，**不是完整 Session**。不能据此宣称“节省了 88.7% 的总 token”，也不能用含重复传输文本的大窗口制造更好看的压缩比。

回填还带来作者后见偏差：事后已经知道什么重要，不代表在线写入时就能做出同样选择。

### 从恢复理解到真正继续工作

早期理解实验中，C 先读三条，主动再读五条后停止，能恢复主要架构边界。但它也曾把“未启动 v0.6-F”部分理解成“下一步应该推进 v0.6-F”。

这是一个实际失败，不应被后续成功抹掉。未完成、待考虑和禁止自动启动，需要明确区分。

Phase 4 的真实续接第一次没有取得可核验完成结果，后来补跑才成功。补跑中 A、B、C 都完成了原子入账校验任务；C 读了三条 Delta，随后读源码、写 patch 并测试。

这个样本有明确局限：任务已经点名 `EvidenceWorkspace.append()`。A 不读历史也能直接定位，所以它证明的是“这条续接路径可以工作”，不是“Delta 显著降低了找代码的成本”。

### H5：不告诉它改哪个文件

H5 让三个 Agent 在同一冻结源码基线上，自己识别当前阶段一个真实、规模适中的缺口，并完成开发：

| 组 | 历史条件 | 项目地图计时下界 |
| --- | --- | ---: |
| A | 不提供 Delta 或 summary，使用源码与提供的 Git metadata | 约 104 秒 |
| B | 既有 authority audit，9,730 字符，随后核实源码 | 约 89 秒 |
| C | 8 条 Delta，约 2,063 返回字符，随后核实源码与 Git metadata | 约 103 秒 |

这些时间来自记录中的检查点，不是精确统一的启动计时，不能据此做速度排名。所谓开放式，也是在预先限定的业务边界内选择工作，而不是毫无约束的项目自治。

三组最终都选择了 native runtime 工具参数校验这个真实缺口，并提交实现和针对性测试。C 的读取是 `3 + 5 = 8` 个任务记录，包含一个空记录，并非八个 change 字符串。

但 C 的 Delta 只覆盖到较早的 v0.6-E，较新的 v0.7 方向是从源码和 Git metadata 恢复的。少量 Delta 可以提供历史入口，却没有独自承担完整的当前状态恢复。

而且三组都不需要用户重新解释背景，也都没有采用废弃方向。这意味着结果支持可行性，却不能把这些共同成功归因于 Delta。任务提示本身也限制了产品阶段并禁止引入 RAG 等机制，不能把遵守这些限制当成纯粹的记忆能力。

实验结论保留为：

> **H5 = PARTIALLY_SUPPORTED**
>
> C 读取 8 条 Delta 后，结合源码、Git 和测试完成真实开放式开发任务；但尚未证明显著减少总源码考古和总 token。

三组的广泛测试都有失败或错误，受到裁剪快照缺少依赖和数据的影响。定向测试通过不等于完整产品回归通过，测试数量也不等于某组质量更高。

## 9. 它和已有工作是什么关系

这个问题并不新。Handoff、项目记忆、进度文件、事件日志和运行时持久化已经从不同方向处理连续性。具体出处与比较见 [PRIOR_ART.md](PRIOR_ART.md)。

PROJECTMEM 记录问题、尝试、修复和决策；Lead-Protocol 明确处理 session 生命周期与交接；Intent 使用 living spec 协调工作；claude-mem 捕获观察并支持后续检索；Mem0 提供记忆操作；LangGraph 提供 checkpoint 和跨任务存储。它们的范围不同，不应被画成同一个“复杂且过时”的对照组。

DeltaLayer 不声称发明了 agent continuity、append-only log 或 handoff。我们尝试验证的是一个更窄的假设：

> **semantic changes 是否可以成为项目连续性的最小原语，并把历史解释交回给 LLM。**

它选择的组合是：

```text
semantic change
    → append-only history
    → recency-first, model-decided reading
    → LLM interprets history
    → current view is disposable
```

一份高质量 handoff 完全可能比它更好。某些既有系统也可能已经实现了相似组合。我们尚未做这些产品的直接对照，欢迎补充相近先例和反例，而不是凭文件数量宣布胜负。

## 10. 接下来需要的不是更多功能，而是更好的反例

当前设计最需要面对的问题包括：

- 在线写入会不会遗漏重要变化，或者逐渐退化成工作日志？
- 模型会不会过早停止回溯，错过很久以前但仍然有效的约束？
- 旧记录与现实冲突时，模型能否稳定核实，而不是擅自认定谁覆盖谁？
- 维护 `PROJECT.md` 的成本，会不会抵消历史输入节省？
- 对跨多个架构阶段的任务，它是否仍然不如一份认真维护的 handoff？

还有一条不能省略的安全边界：日志是历史数据，不是执行授权。来自其他 Agent 或导入材料的“变化”不能升级成新指令；append-only 也不意味着应当永久公开密钥或个人信息。

下一步应在完整、相同的测试环境中，在线生成变化，使用跨两个已演化边界的任务，与源码冷启动和高质量 handoff 做重复对照，同时计算写入、读取、维护与返工成本。若经常需要完整历史才能恢复重要前提，或者没有净收益，就应该收缩甚至放弃这个假设。

现在公开的不是“我们解决了长期 Agent memory”，而是设计、原型、可检查的记录和失败边界。实验包也明确标注了尚未公开的源码快照与原始 Session，不能把它称作完整可复现实验。

也许某些项目的 Agent 连续性确实被过度工程化了。

如果下一任模型已经能够理解时间、矛盾、因果和替代关系，也许基础设施不必提前再解释一遍。

也许，它只需要把变化留下来。

---

继续阅读：[README](README.md) · [冻结的 v0 规范](DESIGN.md) · [实验方法与证据](EXPERIMENTS.md) · [相关工作](PRIOR_ART.md)
